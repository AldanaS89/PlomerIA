[pytest]
# Pytest busca los tests SOLO en la carpeta tests/. Así no agarra archivos
# sueltos como test_gemini.py (que en realidad es un script que llama a la API
# de Gemini y no es una prueba real).
testpaths = tests
